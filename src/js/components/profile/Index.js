import React, {PropTypes} from 'react'; // eslint-disable-line no-unused-vars

const Profile = () => {
   return (
    <div className="panel-container prfile-page">
      <h3>My profile</h3>
    </div>
   )
 }

 Profile.propTypes = {
 }

 export default Profile;
